# Kontrak API: Temuan

Sifat perubahan: **Jarang**: bertambah saat ada temuan.

Ketidakselarasan antara frontend dan backend yang sudah diverifikasi, beserta pemilik dan statusnya. Butir baru ditambahkan dengan nomor berikutnya.

## 6. Ketidakselarasan yang tercatat

Setiap butir di bawah sudah diverifikasi dari kode atau respons backend. Kolom Pemilik menunjukkan sisi yang perlu bertindak. Butir 11 sampai 21 diperiksa terhadap kode backend pada 19 sampai 20 September 2026, dan butir 2, 9, 10, 18, 19, 20, serta 22 dinilai ulang, dan butir 23 sampai 27 diperiksa, terhadap backend `f27f093` (origin/yoga) pada 21 September 2026, bukan terhadap commit acuan di `README.md` bagian 1; nomor barisnya dapat bergeser bila backend berubah.

| No | Temuan | Bukti | Pemilik | Status |
|---|---|---|---|---|
| 1 | Frontend memanggil `/bahan-baku` sebagai percobaan pertama, lalu jatuh ke `/bahanBaku` | Lima panggilan tidak punya pasangan di backend; `GET /bahan-baku` dijawab 404 | Frontend | Dibuang pada Fase 2 |
| 2 | Gate menu sidebar tidak sejalan dengan permission endpoint halamannya | Bagian 5 (`izin-halaman.md`): grup inventaris memakai `read-inventory-outlet` dan `read-inventory-gudang` sebagai gate grup; Pengiriman Stok memakai `read-pengiriman-stok` padahal endpoint mewajibkan `read-transfer-stok`; halaman Pengguna juga butuh `read-role`. Koreksi 21 September 2026: kedua izin inventory itu sempat dicatat tidak diperiksa route mana pun, padahal `GET /inventory` dan `GET /inventory/:id` menerima salah satu dari `read-inventory`, `read-inventory-gudang`, dan `read-inventory-outlet` (`inventoryRoute.js` baris 18 dan 23) | Frontend | Gate diturunkan dari kebutuhan endpoint pada Fase 2; gate stok outlet, bahan baku, dan inventaris gudang menerima izin alternatif sejak `2b3b52d` |
| 3 | Casing path di frontend tidak seragam (`/tipeAset`, `/stockopname`, `/metodepembayaran`) | Seluruhnya berjalan karena Express tidak membedakan huruf besar kecil | Frontend | Konstanta endpoint kanonik pada Fase 2 |
| 4 | Enam permission gate frontend tidak ada di seed (`read-shift-*`, `read-pola-roster-*`, `read-jadwal-shift-*`) | Gate dikomentari di `components/app-sidebar.tsx` | Backend | Catatan backend nomor 2 |
| 5 | Endpoint yang dipakai frontend tanpa `checkPermission` | Tulis: pajak, produk pajak, shift, pola roster, jadwal shift. Baca data sensitif: `GET /laporan/laba-rugi`, `GET /absensi/monitoring`. Baca data referensi transaksi (aset, diskon, metode pembayaran, pelanggan, tarif, tipe aset) perlu dikonfirmasi sebagai desain yang disengaja | Backend | Catatan backend nomor 2, diperluas |
| 6 | Permission sudah ada di seed tetapi tidak diperiksa route mana pun, antara lain `read-laporan`, `read-absensi`, `create-absensi`, `update-absensi`, `read-pelanggan` | Silang seed terhadap route | Backend | Memperkuat butir 5 |
| 7 | Delapan permission membership dipakai route tetapi tidak ada di seed | Route membership dan paket membership | Backend | Sudah tercatat di README backend (G5b) |
| 8 | `GET /api/shift` tanpa query string menjawab 500 `Cannot access 'data' before initialization` | Panggilan langsung ke backend lokal; frontend selalu mengirim query sehingga belum terdampak | Backend | Catatan backend berikutnya |
| 9 | README backend menyatakan validator memakai allowlist, tetapi penolakan field tak dikenal hanya ada di tenant, akun, transfer stok, absensi (absen pulang), pengajuan stok, dan lokasi | Grep pesan "tidak diizinkan" di `validators/`. Koreksi 21 September 2026: inventory sempat tercantum dari `validators/inventoryValidator.js` yang tidak pernah dipanggil dan kini dihapus backend. Grep di `validators/` tidak membuktikan sebuah validator dipakai; pemanggilnya bisa route, controller, atau service (butir 22) | Backend | Catatan dokumentasi backend |
| 10 | 22 operasi tulis tercatat tanpa validator; sebagian meneruskan `req.body` utuh ke service sementara skemanya memuat field sensitif seperti `status`, `disetujuiOleh`, dan `reviewerID` | Bagian 4 (`payload.md`, operasi bertanda skema model). Hitungan ini berasal dari analisis route. Empat operasi stock opname (`POST /stockopname`, `PATCH /stockopname/:id/items`, `.../approve`, dan `.../reject`) ternyata divalidasi dari service (dikoreksi 21 September 2026), sehingga sisanya perlu diperiksa dengan cara yang sama | Backend | Potensi mass assignment, perlu verifikasi di service sebelum dilaporkan sebagai bug |
| 11 | `PUT /produk/:id` memeriksa `if (payload.resep)`, sehingga `resep: []` menjadikan stok 0 dan produk tanpa resep tidak dapat dijual | `produkService` baris 185 dan 209, `inventoryService` sekitar baris 228, trace PUT | Backend | Laporan modul produk dan kategori; frontend hanya mengirim resep bila perlu |
| 12 | Hapus kategori tidak memeriksa produk yang memakainya | `kategoriService.delete` baris 109 sampai 115 | Backend | Laporan modul produk dan kategori; frontend mencegah hapus bila produk dapat dibaca |
| 13 | `kategoriID` produk hanya diperiksa formatnya | `produkValidator` baris 72 sampai 76 | Backend | Laporan modul produk dan kategori; form produk menolak kategori yang tidak ada |
| 14 | Duplikat kategori dijawab 400 dengan field `tenantID` | Trace `POST /api/kategori`; `kategoriService` baris 69 dan 101 | Backend | Laporan modul produk dan kategori; frontend menentukan field dari daftar kategori |
| 15 | Satuan resep produk (5) lebih sempit dari satuan bahan baku (7) | `produkValidator` baris 88, `bahanBakuValidator` | Backend | Laporan modul produk dan kategori; perlu keputusan |
| 16 | Cache daftar produk (TTL 120 detik) tidak dibersihkan saat kategori berubah | `produkService` baris 67 dan 118, `kategoriService` baris 64, 96, dan 113 | Backend | Laporan modul produk dan kategori |
| 17 | Detail produk mengirim `createdAt` dan `updatedAt` bernilai null | Cache kontrak `GET /produk/:param` | Backend | Laporan modul produk dan kategori |
| 18 | Mapper stock adjustment membaca `qtySebelum`, `qtyAdjustment`, `stockOpnameID`, dan `catatan`, padahal model menyimpan `qtyCurrent`, `qtyDifference`, `referenceID`, dan `alasan`; `referenceType` tidak dikirim. Akibatnya saldo sistem dan koreksi selalu 0, sedangkan sumber opname dan alasan selalu null | `mappers/stockOpnameMapper.js` baris 167, 169, 190, 192; `models/stockAdjustmentModel.js` (`qtyCurrent`, `qtyDifference`, `referenceType`); `stockOpnameService` baris 418 sampai 427; cache kontrak `GET /stockopname/adjustments/:param` (`qtySebelum` 0, `qtyPhysical` 35000, `qtyAdjustment` 0) | Backend | Diperbaiki backend di `f27f093`: field mengikuti model, `qtySnapshot` dan `referenceType` dikirim, dan `referenceID` berisi objek hasil populate (`endpoint.md` bagian 3.3). Penanganan sementara frontend dibuang di `2b3b52d` |
| 19 | Pembaruan item stock opname menolak `qtyPhysical` kosong, sehingga hitungan yang baru sebagian tidak dapat disimpan; pesan errornya "tidak boleh kurang dari 0" walau isiannya kosong | `stockOpnameService` sekitar baris 235; frontend lama mengirim `null` untuk isian kosong | Backend | Loop service menerima `null` sejak `f27f093`, tetapi validator di depannya masih menolak (butir 22). Frontend hanya mengirim item yang berubah, selalu dengan hitungan angka, lewat `SERVER_TERIMA_HITUNGAN_KOSONG` di `features/stock-opname/payload.ts` (`2b3b52d`) |
| 20 | Data stock opname dan pengajuan stok (dan data stok lain) dikirim untuk seluruh lokasi tenant kepada pemegang izin baca; pembatasan staf ke lokasi aktif hanya ada di tampilan web | `stockOpnameService.getAll` baris 150 sampai 155: `locationID` hanya filter opsional dari query. `GET /jurnalstok` bahkan tidak membaca query sama sekali (`jurnalStokController`) | Backend, perlu keputusan produk | Laporan submodul stock opname. Backend mencatatnya sebagai SO-3 yang menunggu satu keputusan produk untuk stock opname, inventory, dan jurnal stok sekaligus. Web membatasi staf lewat `useCakupanLokasiOutlet`, dan jurnal stok mengirim `locationID` untuk lingkup satu lokasi sejak `2b3b52d` |
| 21 | Daftar pengajuan stok menyembunyikan status menurut izin: petugas transfer tanpa `approve-pengajuan-stok` hanya menerima SUBMITTED, APPROVED, PENDING, dan COMPLETED, dan status lain dijawab daftar kosong tanpa keterangan, sehingga setiap klien harus mencerminkan aturan ini sendiri | `pengajuanStokService.getAll` baris 30 sampai 47 | Backend | Laporan submodul pengajuan stok; frontend menyembunyikan tab lewat `features/pengajuan-stok/izin.ts` |
| 22 | Pembaruan item stock opname tetap menolak `qtyPhysical` null walau perbaikan SO-1 sudah masuk: `updateItems` menjalankan `validateUpdateItems` sebelum loop pembaruan, dan validator itu menolak `qtyPhysical` null maupun tidak dikirim. Akibatnya hitungan tersimpan tidak dapat dikosongkan, dan mengubah catatan saja ditolak | `stockOpnameService` baris 214 sampai 216, `stockOpnameValidator` baris 60 sampai 64; e2e 21 September 2026: `PATCH /stockopname/:id/items` dengan `qtyPhysical: null` dijawab 400 | Backend | Laporan tindak lanjut 21 September 2026. Frontend mengirim item berubah dengan hitungan angka dan menahan hitungan kosong lewat `SERVER_TERIMA_HITUNGAN_KOSONG`; satu skenario e2e ditandai `test.fixme` (`refactor/pengujian.md`) |
| 23 | Halaman buat dan edit pengajuan stok mengisi `dariLocationID` dengan outlet dan `keLocationID` dengan gudang, kebalikan dari makna backend (gudang asal barang dan outlet peminta), sehingga seluruh pengajuan dari web tersimpan terbalik | Skenario e2e backend (SKENARIO_E2E_TESTING langkah 2) membuat pengajuan dengan gudang di `dariLocationID`; `transferStokService` baris 143 dan 144 menyalin arah ke surat jalan, kirim mengurangi stok di `dariLocationID` (baris 309 dan 330), terima menambah stok di `keLocationID` (baris 388 sampai 407); 8 dari 8 pengajuan development tersimpan `Outlet -> Gudang` | Frontend | Diperbaiki di `08d0a73`; data development dibalik; pengajuan terbalik disembunyikan dari daftar dan dikunci di detail gudang lewat `arahPengajuanValid` |
| 24 | Tipe lokasi pengajuan tidak divalidasi: `create` dan `update` menerima `dariLocationID` dan `keLocationID` tanpa memeriksa bahwa keduanya bertipe Gudang dan Outlet, sehingga pengajuan terbalik diterima diam-diam dan surat jalannya akan memindahkan stok ke arah salah | `pengajuanStokService.create` dan `update`; butir 23 | Backend | Laporan arah lokasi 21 September 2026; frontend mengunci setujui dan surat jalan untuk pengajuan terbalik |
| 25 | Pengajuan APPROVED dan PENDING masih bisa diubah item, jumlah, dan lokasinya, sehingga persetujuan dan pemeriksaan stok tidak mengikat | `pengajuanStokService.update` baris 178 sampai 181: hanya SUBMITTED, COMPLETED, dan REJECTED yang ditolak | Backend | Laporan arah lokasi 21 September 2026; tombol edit web hanya untuk DRAFT, pemeriksaan status di halaman edit ditambahkan saat migrasi |
| 26 | Respons `PATCH /pengajuanstok/:id/approve` berisi dokumen sebelum diperbarui, sehingga statusnya masih SUBMITTED | `pengajuanStokService.approve` mengembalikan `data` hasil pembacaan sebelum `findOneAndUpdate` | Backend | Laporan arah lokasi 21 September 2026; web hanya memakai `message` |
| 27 | Makna `dariLocationID` dan `keLocationID` pengajuan tidak didokumentasikan di model, dan skenario e2e backend masih memakai nama lama `/permintaanstok` dan `permintaanStokID` | `models/pengajuanStokModel.js` baris 16 sampai 24; SKENARIO_E2E_TESTING langkah 2 | Backend | Laporan arah lokasi 21 September 2026 |
